∃∀←→∃∀←→
\342\210\203 \342\210\200 \342\206\220 \342\206\222

λ ƛ π Π

(start-process "stuff" "chars2.txt" "cat" "/Users/rjsimmon/Projects/aeolus/branches/linlog/examples/lispcode.el")(setq a-overlay (make-overlay 1 5))
(setq b-overlay (make-overlay 5 9))

(setq eval-string-string 
  "(progn
   (setq a-buffer (get-buffer \"chars2.txt\"))
   (setq a-overlay (make-overlay 1 5 a-buffer))
   (setq b-overlay (make-overlay 5 9 a-buffer))
   (overlay-put a-overlay 'face '((foreground-color . \"red\")))
   (overlay-put b-overlay 'face '((foreground-color . \"blue\")))
   'Hilit-completed)")
(eval-string () eval-string-string)

(defun eval-string (proc arg)
 (eval (car (read-from-string arg))))
(progn
 (delete-overlay a-overlay)
 (delete-overlay b-overlay)
 'Clear-completed)

(setq proc (start-process "stuff" nil "/Users/rjsimmon/Projects/aeolus/branches/linlog/examples/testeract"))
(set-process-filter proc 'eval-string)
(process-send-string proc "del\n")
(process-send-eof proc)

(load-file "lispcode.el")

(defun eval-string (proc eval-string-string)
(progn
 (setq eval-string-ctr 0)
 (setq eval-string-max (length eval-string-string))
 (while (< eval-string-ctr eval-string-max)
  (progn
   (setq eval-string-cell (read-from-string eval-string-string eval-string-ctr))
   (setq eval-string-car (car eval-string-cell))
   (if (stringp eval-string-car) ;; If eval-string-car is nil
    (setq eval-string-ctr eval-string-max)
    (progn 
     (eval eval-string-car)
     (setq eval-string-ctr (cdr eval-string-cell))))))
 eval-string-car))
